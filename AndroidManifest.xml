<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="intiproject.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".PetInfo">
        </activity>
        <activity android:name=".CatMain">
        </activity>
        <activity android:name=".Shop">
        </activity>
        <activity android:name=".DogMain">
        </activity>
        <activity android:name=".rabbitMain">
        </activity>
        <activity android:name=".fishMain">
        </activity>
        <activity
            android:name=".Pet"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".PetListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".DType">
        </activity>
    </application>

</manifest>